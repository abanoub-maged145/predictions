# إعدادات نشر سيرفر المُتنبئ تلقائياً من GitHub (Cloudflare Workers Builds)
# ------------------------------------------------------------------
name = "predictor"
main = "worker.js"
compatibility_date = "2024-11-01"

# نفس مخزن المزامنة (PREDICTOR) المربوط دلوقتي — بنفس الـ id
# عشان النشر التلقائي ميغيّرش الربط والمزامنة تفضل شغالة
kv_namespaces = [
  { binding = "KV", id = "d69e58f973bd4686848f68eb61d9e215" }
]
